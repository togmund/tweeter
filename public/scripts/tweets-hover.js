// Set a maximum tweet length
const maxTweetLength = 140

// Wait until the document is ready to run scripts
$(document).ready(() => {

  $("article.tweet").on('click', function () {
    // TODOs:
    // Drop Shadow
    // Hide @handle
    // Reduce transparency of all elements
  });

});